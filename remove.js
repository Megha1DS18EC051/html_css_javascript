/*Write a function that takes a string (a) as argument
Remove the first 3 characters of a Return the result*/

function removing(a)
{
return a.substring(3);//a.slice(3)
}

console.log(removing("Megha"));